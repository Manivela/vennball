FROM node:22-alpine
WORKDIR /app/server
RUN echo '{"type":"module"}' > /app/package.json && npm install --prefix /app ws
COPY shared/ /app/shared/
COPY server/game-server.js ./game-server.js
ENV PORT=8080
EXPOSE 8080
CMD ["node", "game-server.js"]
